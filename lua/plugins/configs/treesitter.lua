local options = {
	ensure_installed = { "lua", "tsx", "css", "html", "rust", "javascript" },

	highlight = {
		enable = true,
		use_languagetree = true,
	},
	indent = {
		enable = true,
	},
}

return options
