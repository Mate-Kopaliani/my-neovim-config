local options = {
	ensure_installed = { "lua", "tsx", "css", "html", "toml", "rust", "javascript", "java" },

	highlight = {
		enable = true,
		use_languagetree = true,
	},
	indent = {
		enable = true,
	},
}

return options
