local options = {
	ensure_installed = { "lua", "tsx", "css", "html", "toml", "rust", "typescript", "javascript" },

	highlight = {
		enable = true,
		use_languagetree = true,
	},
	indent = { enable = true },
}

return options
